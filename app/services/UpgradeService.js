import { AppState } from "../AppState.js"

class UpgradeService{


    collectRocks(){rocks += AppState.player.rocksOnClick}

    autoRocks(){rocks += AppState.player.autoRocks}
    
}

export const upgradeService = new UpgradeService()
